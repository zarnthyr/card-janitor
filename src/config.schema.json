{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "config_version",
    "notify_after_automatic_run",
    "warn_on_invalid_automatic_policies",
    "debug_logging"
  ],
  "properties": {
    "automatic_cleanup_enabled": {
      "type": "boolean",
      "default": true,
      "description": "Enable automatic cleanup; manual cleanup remains available"
    },
    "cleanup_history_enabled": {
      "type": "boolean",
      "default": true,
      "description": "Record an append-only cleanup history in local add-on files"
    },
    "config_version": {
      "const": 1
    },
    "notify_after_automatic_run": {
      "type": "boolean",
      "description": "Show a summary when cards are cleaned up automatically"
    },
    "warn_on_invalid_automatic_policies": {
      "type": "boolean",
      "default": true,
      "description": "Warn on profile open when automatic policies cannot run"
    },
    "debug_logging": {
      "type": "boolean",
      "description": "Print policy evaluation diagnostics to the terminal"
    }
  }
}
